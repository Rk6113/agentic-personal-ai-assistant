[package]
name = "assistant-core"
version = "0.1.0"
edition = "2021"
description = "Rust assistant core — policy gate, tool registry, scheduler"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
