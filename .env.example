# Create .env at repo root (never commit). Services read from root .env.

OPENWEATHER_API_KEY=
OPENAI_API_KEY=
DATABASE_URL=postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/agentic_assistant?sslmode=require

# Gmail OAuth 2.0 (read-only) — create credentials at https://console.cloud.google.com/apis/credentials
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REDIRECT_URI=http://localhost:8001/gmail/oauth/callback
