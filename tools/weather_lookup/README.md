# weather_lookup

Fetches current weather and forecast via the OpenWeatherMap API to generate
outfit and commute recommendations.

## Status

Skeleton — `spec.json` defines the contract. Implementation pending.

## Risk Level

**Low** — external read-only API call, no user data modified.
