# email_event_reader

Reads the user's Gmail inbox (read-only OAuth) and extracts calendar events,
deadlines, and reminders.

## Status

Skeleton — `spec.json` defines the contract. Implementation pending.

## Risk Level

**Low** — read-only access, no side effects.
